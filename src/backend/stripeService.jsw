// ============================================================================
// STRIPE SERVICE (PUBLIC API)
// This module exports only the functions required by the frontend.
// Sensitive logic and webhook handlers are moved to 'backend/stripeCore'
// to prevent unauthorized access via Velo web methods.
// ============================================================================

import {
  createPlacementDepositCheckout,
  getPublishableKey,
  getCheckoutSession,
  createCheckoutSession,
  createPortalSession
} from 'backend/stripeCore';

// Export functions for frontend use
export {
  createPlacementDepositCheckout,
  getPublishableKey,
  getCheckoutSession,
  createCheckoutSession,
  createPortalSession
};
