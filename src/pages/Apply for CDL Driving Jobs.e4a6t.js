/**
 * Apply for CDL Driving Jobs Page Code
 * Landing page with embedded JotForm for driver applications.
 *
 * No postMessage bridge - the HTML is a static marketing/landing page
 * with an external JotForm iframe for collecting applications.
 */

$w.onReady(function () {
    // Static landing page - no HTML component bridge needed
});
