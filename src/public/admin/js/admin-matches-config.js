/* =========================================
   ADMIN MATCHES — Config Module
   No dependencies
   ========================================= */
var AdminMatchesConfig = (function () {
  'use strict';

  var THEME_KEY = 'lmdr-admin-theme';

  return {
    THEME_KEY: THEME_KEY
  };
})();
